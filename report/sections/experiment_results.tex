En la tabla de resultados de tiempo de ejecucion podemos observar como la funcion Sort implementada en C++ es la mas rapida, seguida por el algoritmo de ordenamiento quicksort, y luego por mergesort. Esto se debe a que la funcion Sort esta optimizada y utiliza algoritmos de ordenamiento eficientes, la implementacion de quicksort presento problemas y se estanco en los datos de orden 1000 y finalmente mergesort obtiene el tercer lugar, por gran diferencia. No se logro verificar el motivo que impidio el correcto funcionamiento la implementacionde quicksort.
Por otro lado, en la tabla de resultados de uso de memoria solo se registro el uso de memoria de la fucion sort, aunque se sospecha que no hubo un correcto registro de los otros dos algoritmos pues en los archivos de metricas generados por "makefile", aparece el uso de memoria de los tres algoritmos y ocupan los mismos numeros, lo cual no corresponde a la teoria de estos algoritmos. Quicksort es una funcion inplace y solo ocupa memoria para las llamadas recursivas, mientras que merge sort si utiliza memoria adicional para almacenar los subarreglos durante el proceso de ordenamiento. Por lo tanto, se esperaba que merge sort tuviera un mayor uso de memoria en comparación con quicksort. Sin embargo, debido a los problemas con la implementacion de quicksort, no se pudo obtener una medicion precisa del uso de memoria para este algoritmo.

Los resultados preeliminares de la multiplicacion de matrices presentaban muchos errores y gran varianza lo cual implicaba una mala implementacion de los metodos y algoritmos. 